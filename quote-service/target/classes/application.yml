server:
  port : 0
spring:
  application:
    name: quote-service
  profiles:
    active: development
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
springdoc:
  api-docs:
    path : /quote-doc
  swagger-ui:
    path: /quote-swagger-ui.html
#  instance:
#    preferIpAddress: true